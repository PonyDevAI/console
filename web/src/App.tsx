export default function App() {
  return (
    <main>
      <h1>Console UI (stub)</h1>
      <p>
        This placeholder will evolve into workspace, repository, and worker selection,
        plus chat-triggered runs with streaming output.
      </p>
    </main>
  );
}
